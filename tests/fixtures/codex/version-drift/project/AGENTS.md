# Version drift fixture project

Pinned to Codex CLI 0.131.0 — above the matrix maxVersion for `settings.knownKeysOnly` (0.130.0) — so settings unknownFields are stripped while instruction.chain stays enforced.
