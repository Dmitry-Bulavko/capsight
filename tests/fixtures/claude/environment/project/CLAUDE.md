# Environment fixture project

`env.json` sets §3.11 process variables and `.claude/settings.json` carries an
`env` block whose values are secrets. Only key names may reach the snapshot
(§13 invariant 10).
