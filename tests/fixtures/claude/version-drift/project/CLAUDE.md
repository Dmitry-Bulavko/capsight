# Version drift fixture project

Pinned to Claude Code 2.1.500 — above the matrix maxVersion for `agent.tools` (2.1.499) — so tools whitelist verdicts downgrade to unknown while permission:default stays enforced.
