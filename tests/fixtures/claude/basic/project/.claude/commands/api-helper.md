---
description: Command that loses its name to the api-helper skill
---

`.claude/skills/api-helper/SKILL.md` declares the same name and takes
precedence, so this file is not what the name resolves to (K11).
