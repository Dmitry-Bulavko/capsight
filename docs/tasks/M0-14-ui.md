# M0-14: UI discovery viewer

## Goal

React UI showing discovery results: agents with source/scope/status, invalid reasons, version, scan trigger.

## Spec refs

- SPEC §10 M0 goal — first useful screen on foreign repo
- SPEC §12.4 M0 API

## Scope IN

- `src/ui/App.tsx`
- `src/ui/components/` (AgentList, ScanPanel, ProjectSummary — new)
- `src/ui/styles.css`
- `src/ui/api.ts` (fetch helpers for /api/project/scan, /api/project, /api/agents)

## Scope OUT

- Context selector, Why panel (M1)
- Graph (M2)
- Resolver UI

## Acceptance

- [ ] On load: prompt for scan or auto-scan cwd via POST /api/project/scan
- [ ] Shows Claude version or "unknown"
- [ ] Lists all agents: name, status, scope, file path
- [ ] Invalid agents show invalidReason
- [ ] Ambiguous/shadowed agents visually distinct
- [ ] Shows counts: skills, instructions, MCP servers
- [ ] Read-only — no edit controls
- [ ] `npm run test` and `npm run typecheck` pass

## Done checklist

- [ ] npm run test
- [ ] npm run typecheck
